# Reduce install work in preview builds.
# canvas is an optional dependency (native build) that can cause install timeouts.
optional=false

# Keep installs quiet & faster (optional)
audit=false
fund=false
