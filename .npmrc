# Production build settings for Vercel
legacy-peer-deps=true
audit=false
fund=false

# IMPORTANT: Do NOT set optional=false - this breaks Rollup native binaries
# Rollup requires optional dependencies like @rollup/rollup-linux-x64-gnu