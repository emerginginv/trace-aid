# Prevent native optional binaries (fixes rollup-linux-x64-gnu)
optional=false

# Reduce noise / speed up CI
audit=false
fund=false
progress=false

# Dependency safety
legacy-peer-deps=true

# Network stability
fetch-retries=2
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=60000